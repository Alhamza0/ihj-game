# سجلّ التغييرات — إنسان · حيوان · جماد

كل الإصدارات المهمّة موثّقة هنا. يتبع المشروع [الإصدار الدلالي (SemVer)](https://semver.org/lang/ar/):
`رئيسي.ثانوي.تصحيحي` — رئيسي لتغييرات كاسرة، ثانوي لميزات جديدة، تصحيحي للإصلاحات.

> **عند إصدار نسخة:** (١) ارفع `version` في `package.json` و`client/package.json`؛
> (٢) طابِق `CACHE_VERSION` في `client/public/sw.js` معه (`ihj-vX.Y.Z`) كي يكتشف المتصفح التحديث؛
> (٣) حدّث `client/public/version.json` (`version` + `notes` ملخّص يظهر في إشعار التحديث)؛ (٤) أضف قسماً هنا.

## [1.4.0] — 2026-06-04
### أُضيف
- **حفظ التدريب الفردي:** مباريات الوضع المحلي «تدريب فردي» تُحفظ الآن في الليدربورد للاعب المسجّل
  (نقاط + عدد مباريات، **دون** احتساب «فوز» لعدم وجود منافس). عبر مسار خادم `POST /solo` يتحقّق من JWT.
  الوضع الجماعي المحلي يبقى بلا حفظ. (مُسوَّر بالبيئة كبقية ميزات الحسابات.)

## [1.3.1] — 2026-06-04
### أُصلح
- شاشة الملف الشخصي: استعلام `profiles` لم يعد يُصدر خطأ `406` في الكونسل عندما لا يوجد صفّ للاعب
  بعد (قبل أوّل مباراة) — استُبدل `.single()` بجلب آمن.

## [1.3.0] — 2026-06-04
### أُضيف
- **حسابات لاعبين عبر Supabase:** تسجيل دخول **اختياري** بـ Google على شاشة الانضمام (يبقى اللعب كضيف متاحاً).
- **حفظ المباريات:** الخادم الموثوق يكتب نتيجة كل مباراة (`matches` + `match_players`) إلى Supabase
  بمفتاح service role؛ الواجهة تقرأ فقط (anon + RLS) فلا يمكن تزوير النقاط.
- **ليدربورد عام:** تصنيف يومي/أسبوعي/كل الأوقات (دالة `leaderboard` في Postgres).
- **ملف لاعب عام:** إحصاءات (نقاط/مباريات/انتصارات/أعلى نتيجة) + آخر المباريات.
- **توافق خلفي:** بدون متغيّرات Supabase تعمل اللعبة كما هي بلا حسابات (التطوير المحلي + اختبار e2e).
- إعداد موثّق في `SUPABASE_SETUP.md` ومخطّط القاعدة في `supabase/schema.sql`.

## [1.2.0] — 2026-06-02
### أُضيف
- **ترقيم إصدارات موثّق:** مصدر واحد لرقم النسخة (`package.json`) يُحقَن في الواجهة عبر Vite (`__APP_VERSION__`)،
  ويُعرَض في تذييل الشاشة الرئيسية مع حقوق النشر.
- **حقوق النشر:** اعتماد «تصميم وتطوير اللعبة — حمزة إحسان علي» + تذييل `© 2026 · جميع الحقوق محفوظة`
  + ملف `LICENSE` (احتكاري — جميع الحقوق محفوظة).
- **إشعار توفّر تحديث (PWA):** عند نشر نسخة جديدة، تكتشفها الواجهة وتعرض شريط «🎉 نسخة جديدة متوفّرة — حدّث الآن»؛
  الضغط يفعّل الـ service worker الجديد ويعيد التحميل تلقائياً. فحص دوري وعند العودة إلى التبويب.
- **ملخّص التحديث:** يُجلب من `version.json` لنسخة الخادم الجديدة ويُعرَض داخل الإشعار كقائمة بأبرز التغييرات.

## [1.1.0] — 2026-06-02
### أُضيف
- **قابلية التثبيت كتطبيق (PWA):** `manifest.webmanifest` + service worker يدويان (بلا اعتمادات جديدة).
- **عمل أوفلاين:** كاش قشرة التطبيق؛ وضع اللعب المحلي يعمل بلا إنترنت (طلبات Socket.IO تُمرَّر للشبكة دائماً).
- **صمود إجابات الجوال:** حفظ مسوّدة محلياً أثناء الكتابة واستعادتها لنفس الجولة؛ إعادة إرسال تلقائية
  للإجابات المعلّقة عند عودة الاتصال؛ حفظ عند قفل الشاشة/تبديل التطبيق.
- **تجربة لمس أفضل:** حقول أكبر، تنقّل بمفتاح Enter بين الحقول، مؤشّر «غير متصل» أثناء اللعب.

## [1.0.0] — 2026-06-01
### أُضيف
- إعادة بناء اللعبة من ملف HTML واحد إلى مشروع منظّم بخادم موثوق (Socket.IO) بديلاً عن PeerJS.
- وضعان: لعب محلي على جهاز واحد، ولعب جماعي أونلاين (مضيف «تلفاز» + لاعبون عبر QR).
- محرك نقاط متطوّر (حالات تحقّق + مكافأة سرعة عادلة) وفئات جديدة وتعليق صوتي عربي.
- نشر حيّ مجاني على Render (الخادم) و Vercel (الواجهة) مع نشر تلقائي عند الدفع إلى `main`.
- اختبار E2E على البروتوكول (`server/e2e-test.mjs`).
